defmodule UndiWeb.Admin.InitAssigns do
  @moduledoc """
  Ensures common `assigns` are applied to all LiveViews attaching this hook.
  """
  use UndiWeb, :verified_routes

  # import Phoenix.Component
  # import Phoenix.LiveView

end
